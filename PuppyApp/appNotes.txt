FLASK_APP = apuppy_dashboard.py
FLASK_VENV = myflaskenv
(You can make your own venv and pip install the env_requirements.txt file)
	
	(conda create -n myflask env python=3.6)
		(activate myflaskenv)
	(pip install -r env_requirements.txt)